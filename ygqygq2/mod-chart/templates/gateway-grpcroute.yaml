{{- if and .Values.gateway.enabled .Values.gateway.grpc.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: GRPCRoute
metadata:
  name: {{ include "common.names.gateway.grpcroute" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
  {{- if or .Values.gateway.grpc.annotations .Values.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.gateway.grpc.annotations .Values.commonAnnotations ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  {{- include "common.gateway.parentRefs" (dict "gateway" .Values.gateway "context" $) | nindent 2 }}
  {{- if .Values.gateway.grpc.hostnames }}
  hostnames:
    {{- range .Values.gateway.grpc.hostnames }}
    - {{ . | quote }}
    {{- end }}
  {{- end }}
  rules:
{{- include "common.gateway.grpcroute.rules" (dict "rules" .Values.gateway.grpc.rules "serviceName" (include "common.names.fullname" .) "servicePort" .Values.service.ports.grpc.port "context" $) | nindent 4 }}
{{- end }}
