{{- if and .Values.gateway.enabled .Values.gateway.http.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "common.names.gateway.httproute" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
  {{- if or .Values.gateway.http.annotations .Values.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.gateway.http.annotations .Values.commonAnnotations ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  {{- include "common.gateway.parentRefs" (dict "gateway" .Values.gateway "context" $) | nindent 2 }}
  {{- if .Values.gateway.http.hostnames }}
  hostnames:
    {{- range .Values.gateway.http.hostnames }}
    - {{ . | quote }}
    {{- end }}
  {{- end }}
  rules:
  {{- include "common.gateway.httproute.rules" (dict "rules" .Values.gateway.http.rules "serviceName" (include "common.names.fullname" .) "servicePort" .Values.service.ports.http.port "context" $) | nindent 2 }}
{{- end }}
